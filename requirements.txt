# Core agents framework
strands-agents>=0.1.0
strands-agents-tools>=0.1.0
strands-agents[litellm]

# LLM routing & Groq SDK
litellm>=1.40.0
groq>=0.9.0

# Data processing
pandas>=2.0.0
numpy>=1.26.0

# Web server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# AWS (optional – only needed for Bedrock backend, commented out for Vercel deployment)
# boto3>=1.34.0

# Env loading (fallback for platforms that don't inject env natively)
python-dotenv>=1.0.0
